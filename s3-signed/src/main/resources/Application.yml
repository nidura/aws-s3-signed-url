cloud:
  aws:
    credentials:
      access-key: 
      secret-key:
    region:
      static: eu-west-2
    stack:
      auto: false

application:
  bucket:
    name: s3-test-without-signed-url


spring:
  servlet:
    multipart:
      enabled: true
      file-size-threshold: 2MB
      max-file-size: 5MB
      max-request-size: 10MB

